/node_modules
/data/
# NOTE: public/vendor/ (the built Excalidraw bundle) is deliberately TRACKED,
# not ignored. node_modules is locked down (package-lock + integrity hashes +
# npm ci) so it need not be committed; the build OUTPUT has no such lock and npm
# packages can be yanked, so we commit the approved artifact to guarantee access.
/comfyui-output.png
/private/
Rivers_of_London/test_write
Rivers_of_London/rules/source/raw/
tim.json
tim.pdf
verify_output.pdf
**/__pycache__/
*.pyc
Rivers_of_London/cha3200_-_rivers_of_london_1.4_artifacts/
