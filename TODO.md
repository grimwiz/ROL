# TODO

- Populate session NPCs from rulebook/source material with plausible skills, stats, backgrounds, and portraits in the same spirit as player characters.
- Rework the **GM Info** tab. It is currently just the raw `gm-analysis.json` categories dumped as cards and is disappointing to use. It needs a proper GM-facing design: a per-player view mirroring the Player Info layout, scenario-pacing at-a-glance, actionable next steps, and integration with the fairness/engagement and quiet-player signals rather than flat lists.
